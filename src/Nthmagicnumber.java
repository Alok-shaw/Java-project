import java.util.Arrays;
import java.util.Map;
import java.util.Scanner;

public class Nthmagicnumber {
    public static void main(String[] args) {
       Scanner in=new Scanner(System.in);
       System.out.println("Enter your number  :");
       int a=in.nextInt();
       StringBuilder sb=new StringBuilder();
       // first we convert the decimal to binary in manually
//        String b1=Integer.toBinaryString(a);
//        int num=Integer.parseInt(b1);
//        char[] ch=b1.toCharArray();
//        System.out.println(Arrays.toString(ch));
//        int sum=0;
//        int k=0;
//        for(int i=ch.length-1;i>=0;i--){
//            int bit=ch[i]-'0';
//            sum=sum+bit*(int)Math.pow(2,k);
//            k++;
//        }
//        System.out.println(sum);
//        //if() i am doing everything using in-built function so now i decide to do everything in manually

    }
}
